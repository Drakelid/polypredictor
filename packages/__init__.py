"""Top-level package for PolyPredictor shared packages.

This file is intentionally kept minimal to allow Python to treat the
`packages` directory as a namespace package. Individual sub-packages
such as `model` provide the actual implementation.
"""
