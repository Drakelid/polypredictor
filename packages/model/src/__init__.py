"""Source code for the model package.

This intermediate `src` directory mirrors the repository structure used
in the upstream PolyPredictor project. It contains the actual
implementation of the model components such as classifiers, ensemble
layers, and utilities. Keeping the code under `src` helps avoid
import conflicts when the package is installed as an editable
distribution.
"""
